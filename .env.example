# Muffin Pan Recipes - Environment Variables
#
# All secrets are managed via Doppler (project: muffinpanrecipes, configs: prd/stg/dev).
# On Vercel they are injected automatically via Doppler integration.
# Locally: `doppler run -- uv run python -m backend.main`
# Or copy this file to .env.local and fill in values manually.
#
# Doppler quickstart:
#   brew install dopplerhq/cli/doppler
#   doppler login
#   doppler setup              # select muffinpanrecipes / dev
#   doppler run -- <command>   # injects all secrets as env vars
#   doppler secrets             # list all secrets for current config

# --- Local Development ---
# LOCAL_DEV=true              # Bypasses Google OAuth; returns dummy session. NEVER set in production.

# --- AI Model Keys ---
OPENAI_API_KEY=               # OpenAI API key (dialogue, recipes, vision eval)
ANTHROPIC_API_KEY=            # Anthropic API key (judge pipeline, fallback provider)
STABILITY_API_KEY=            # Stability AI image generation
GEMINI_API_KEY=               # Google Gemini (not yet integrated — reserved for #5034)

# --- Model Selection ---
# These control which model each pipeline stage uses.
# Format: "provider/model" e.g. "openai/gpt-5.1", "anthropic/claude-haiku-4-5"
DIALOGUE_MODEL=               # Weekly dialogue generation (default: anthropic/claude-haiku-4-5)
RECIPE_MODEL=                 # Baker recipe generation (default: openai/gpt-5.1)
JUDGE_MODEL=                  # Opus judge pipeline (default: anthropic/claude-opus-4-6)
VISION_EVAL_MODEL=            # Art Director image evaluation (default: openai/gpt-5-mini)

# --- Authentication ---
GOOGLE_CLIENT_ID=             # Google OAuth client ID
GOOGLE_CLIENT_SECRET=         # Google OAuth client secret
GOOGLE_REDIRECT_URI=          # OAuth callback URL (e.g. https://muffinpanrecipes.com/auth/callback)
GOOGLE_AUTHORIZED_EMAILS=     # Comma-separated allowed admin emails
JWT_SECRET=                   # Session token signing key (generate: openssl rand -hex 32)

# --- Vercel / Infrastructure ---
CRON_SECRET=                  # Vercel cron auth token (auto-generated by Vercel)
BLOB_READ_WRITE_TOKEN=        # Vercel Blob storage token (episode persistence across Lambda invocations)
MUFFINPAN_ADMIN_BASE_URL=     # Admin dashboard URL for Discord links (e.g. https://muffinpanrecipes.com)

# --- Notifications ---
MUFFINPAN_DISCORD_WEBHOOK=    # Discord webhook URL for pipeline notifications

# --- Publishing Schedule (DST-aware local market clock) ---
MUFFINPAN_PUBLISH_TIMEZONE=America/Los_Angeles
MUFFINPAN_PUBLISH_WEEKDAY=6   # 0=Mon ... 6=Sun
MUFFINPAN_PUBLISH_HOUR=17
MUFFINPAN_PUBLISH_MINUTE=0

# Muffin Pan Recipes - Isolated Credentials (Template)
# Pattern: project-name-service

# --- Local Development ---
# LOCAL_DEV=true   # Bypasses Google OAuth in require_auth(); returns dummy session. NEVER set in production.

# --- Vercel (Deployment) ---
# VERCEL_TOKEN=
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=

# --- RunPod (Image Generation) ---
# MUFFINPANRECIPES_RUNPOD_API_KEY=

# --- Cloudflare R2 (Image Storage & Sync) ---
# MUFFINPANRECIPES_R2_ACCESS_KEY_ID=
# MUFFINPANRECIPES_R2_SECRET_ACCESS_KEY=
# MUFFINPANRECIPES_R2_ENDPOINT=
# MUFFINPANRECIPES_R2_BUCKET_NAME=

# --- OpenAI (Dialogue) ---
# OPENAI_API_KEY=
# OPENAI_MODEL_ALLOWLIST=gpt-5-mini,gpt-5-nano,gpt-5.1
# NOTE: Owner policy is strict: only gpt-5-mini, gpt-5-nano, gpt-5.1 are allowed.
# NOTE: Models not in OPENAI_MODEL_ALLOWLIST are blocked in code.

# --- Publishing Schedule (DST-aware local market clock) ---
# MUFFINPAN_PUBLISH_TIMEZONE=America/Los_Angeles
# MUFFINPAN_PUBLISH_WEEKDAY=6
# MUFFINPAN_PUBLISH_HOUR=17
# MUFFINPAN_PUBLISH_MINUTE=0
